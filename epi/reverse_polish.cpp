#include <iostream>
#include <stack>
#include <string>

using namespace std;

// RPN : Reverse Polish Notation
class RPNCalculator {
private:
    string input;
    stack<char> 
public:
    RMPCalculator(string s) : input(s) {}
};
